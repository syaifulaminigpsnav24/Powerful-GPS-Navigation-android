<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PowerfulGPSNavigation" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.PowerfulGPSNavigation" parent="Base.Theme.PowerfulGPSNavigation" />

    <style name="Theme.PowerfulGPSNavigation.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.PowerfulGPSNavigation.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.PowerfulGPSNavigation.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>